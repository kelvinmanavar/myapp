pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {

                // Build the Docker image
                sh 'docker compose up -d --build '

               // Run the Docker container with the new image
                               
            }
        }
    }
}
